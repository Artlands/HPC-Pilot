name: triage-node-down
description: Investigate why a compute node is down — check Slurm, controller logs, fabric, and produce a recommendation.
required_role: operator
inputs:
  - name: node
    type: string
    required: true
steps:
  - id: snapshot
    tool: hpc_slurm_node_status
    args: {node: "{{ node }}"}
  - id: sdiag
    tool: hpc_slurm_sdiag
    args: {}
  - id: slurmctld_tail
    tool: hpc_logs_slurmctld_tail
    args: {}
  - id: slurmd_tail
    tool: hpc_logs_slurmd_tail
    args: {node: "{{ node }}"}
  - id: dmesg
    tool: hpc_logs_dmesg_xid
    args: {node: "{{ node }}"}
  - id: fabric
    tool: hpc_fabric_ib_link_status
    args: {node: "{{ node }}"}
  - id: recommendation
    builtin: for_each
    args: {}
on_failure: pause
